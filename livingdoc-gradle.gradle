apply plugin: 'groovy'

version = LIVINGDOC_VERSION
group = "info.novatec.testit"

repositories {
    mavenCentral()
}

dependencies {
	compile gradleApi()
	compile localGroovy()
    compile "info.novatec.testit:livingdoc-core:${LIVINGDOC_VERSION}@jar"
}